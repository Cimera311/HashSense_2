// ============================================
// Admin Config - LOCAL ONLY (not in Git)
// Copy admin-config.example.js to admin-config.js
// and fill in your service_role key
// ============================================

const ADMIN_SECRETS = {
    serviceRoleKey: 'YOUR_SERVICE_ROLE_KEY_HERE',
    adminEmails: [
        'sebmiller@googemail.com'
    ]
};
